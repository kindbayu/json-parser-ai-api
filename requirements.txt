# ============================================================
# AI Engine API v3.0
# Universal Document Intelligence — FastAPI + LangChain
# Deploy: Railway / Render (free tier, 512 MB RAM)
#
# Embedding: chromadb built-in DefaultEmbeddingFunction
# (onnxruntime-based, ~50 MB — no PyTorch, no sentence-transformers)
# ============================================================

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Data validation
pydantic==2.11.5

# LangChain core + community
langchain==0.3.25
langchain-community==0.3.24

# LLM providers
langchain-groq==0.3.2
langchain-ollama==0.3.3

# Vector database (includes onnxruntime embedding, no PyTorch needed)
chromadb==0.6.3

# PDF processing
pypdf==5.4.0

# Environment variables
python-dotenv==1.1.0

# Form & file upload
python-multipart==0.0.20

# PDF generation
reportlab==4.4.1
